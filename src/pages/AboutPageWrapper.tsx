import { useNavigate } from "react-router-dom";
import AboutPageContent from "@/pages/AboutPage";

// This is a wrapper - but actually let me just make the pages self-contained
export default AboutPageContent;
